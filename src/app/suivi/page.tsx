"use client";

import { Carte, Vide } from "@/components/ui";

/** Écran « Suivi » — en cours de construction. */
export default function PageSuivi() {
  return (
    <Carte>
      <Vide
        icone="📈"
        titre="Suivi"
        texte="Cet écran arrive à l'étape suivante."
      />
    </Carte>
  );
}
