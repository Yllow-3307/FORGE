"use client";

import { Carte, Vide } from "@/components/ui";

/** Écran « Profil » — en cours de construction. */
export default function PageProfil() {
  return (
    <Carte>
      <Vide
        icone="📝"
        titre="Profil"
        texte="Cet écran arrive à l'étape suivante."
      />
    </Carte>
  );
}
