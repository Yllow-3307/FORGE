"use client";

import { Carte, Vide } from "@/components/ui";

/** Écran « Programme » — en cours de construction. */
export default function PageProgramme() {
  return (
    <Carte>
      <Vide
        icone="📆"
        titre="Programme"
        texte="Cet écran arrive à l'étape suivante."
      />
    </Carte>
  );
}
